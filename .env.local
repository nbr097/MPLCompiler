# .env.local
# Point this at a real parser service if you have one, or at the mock below.
PARSER_URL=https://inventory-pdf-parser.fly.dev/parse

# If your parser needs a token, add it (optional)
PARSER_AUTH=

# Optional: tweak timeout (ms)
PARSER_TIMEOUT_MS=90000